#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main - a program
 * @argc: the count of the argument received
 * @argv: the array of string
 * Return: void
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
